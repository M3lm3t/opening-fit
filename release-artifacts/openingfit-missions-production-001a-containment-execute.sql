-- PRODUCTION WARNING: target frtjfvhiimgruenqcuon only; Mission 001A ordinary-client privilege containment.
-- Do not rerun after an uncertain result; run the read-only containment verification first.
BEGIN;
DO $precondition$ begin if not ((select count(*) from pg_class r join pg_namespace n on n.oid=r.relnamespace where n.nspname='public' and r.relkind='r' and r.relname=any(array['openingfit_missions','openingfit_mission_training_attempts','openingfit_mission_encounters','openingfit_mission_status_events']))=4) then raise exception '001A containment requires all four exact tables'; end if; end $precondition$;
revoke all on table public.openingfit_missions,public.openingfit_mission_training_attempts,public.openingfit_mission_encounters,public.openingfit_mission_status_events from public,anon,authenticated;
DO $assert$ begin if not (not exists(select 1 from (values('openingfit_missions'),('openingfit_mission_training_attempts'),('openingfit_mission_encounters'),('openingfit_mission_status_events')) t(name) cross join (values(0::oid),('anon'::regrole::oid),('authenticated'::regrole::oid)) r(oid) cross join (values('SELECT'),('INSERT'),('UPDATE'),('DELETE'),('TRUNCATE'),('REFERENCES'),('TRIGGER')) p(name) where has_table_privilege(r.oid,to_regclass('public.'||t.name),p.name)) and (select count(*) from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname=any(array['openingfit_missions','openingfit_mission_training_attempts','openingfit_mission_encounters','openingfit_mission_status_events']) and c.relrowsecurity)=4 and (select count(*) from public.openingfit_missions)+(select count(*) from public.openingfit_mission_training_attempts)+(select count(*) from public.openingfit_mission_encounters)+(select count(*) from public.openingfit_mission_status_events)=0) then raise exception '001A containment postcondition failed'; end if; end $assert$;
COMMIT;
